# Журнал изменений (CHANGELOG) — Centr-Krasok AI Bot

## [2026-05-28] — Финал и запуск MVP RAG-бота
### Выполнено:
- **[x] Локальная база знаний (RAG):** Написан модуль `vectorStore.ts` для автоматической нарезки, векторизации через `text-embedding-004` и семантического косинусного поиска релевантного контекста.
- **[x] Сбор данных:** Интегрирован парсер `parser.ts` (Axios + Cheerio) для выгрузки реального контента с сайта `centr-krasok.kz`. Наполнены файлы `about.txt`, `products.txt`, `contacts.txt` (с точными адресами бутиков в ТК ARMADA).
- **[x] Контекст диалога (Session Memory):** Реализовано сохранение сессий `sessions` для удержания истории диалога (до 8 реплик) и обогащение поисковых запросов в RAG на основе истории.
- **[x] Исключение обрезания слов (Chunk Overlap):** Нарезка текстовой базы переписана с фиксированным нахлестом (overlap) в 200 символов, что исключает разрыв слов на стыках чанков.
- **[x] Защита от разметки:** Убраны все символы `*` и `**` из ответов на уровне промпта и программной регулярной очистки.
- **[x] Вежливый перевод на менеджера:** При отсутствии ответа бот предлагает перенаправить диалог на человека.
- **[x] Отказоустойчивый Docker-деплой:** Создан мультистейдж `Dockerfile`, проект успешно развернут на сервере под управлением Coolify (Self-hosted).
